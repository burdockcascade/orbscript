function main()

    -- set a to true
    var a = true

    -- if true then set a to false
    if 1 == 2 then
        a = false
    end

    -- assert a is true
    assertTrue(a, "a should be true")

end
